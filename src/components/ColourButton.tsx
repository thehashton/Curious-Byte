import React from "react";
import css from './ColourButton.scss';

const ColourButton: React.FunctionComponent = () => {
    return (
        <button className={css.ColourButton}>button</button>
    )
}

export default ColourButton;